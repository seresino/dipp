// import type { HandleClientError } from "@sveltejs/kit";

// export const handleError: HandleClientError = ({ error, event }) => {
// 	return {
// 		message: "An unexpected error occurred. We're working on it!",
// 	};
// };

// Check if a client error handler is needed - may be necessary for when data doesn't load properly/is missing
// But this shouldn't be possible if we've design database operations and client logic properly.
